name: whole-stack tests

# A push to parsifal runs the whole-stack gate (owned by cg-from-parse) against
# THIS repo's pushed code plus current cgraph and cg-from-parse, each suite in
# its own SBCL process. Self-contained: no cross-repo token needed -- the two
# dependency repos are public and checked out directly.

on:
  push:
    branches: [ main ]
  pull_request:
  workflow_dispatch:

jobs:
  whole-stack:
    runs-on: ubuntu-latest
    env:
      # Make every checked-out repo ASDF-discoverable; ql:quickload falls back to
      # ASDF for systems not in the Quicklisp dist.
      CL_SOURCE_REGISTRY: '(:source-registry (:tree "${{ github.workspace }}") :inherit-configuration)'
      # PINNED so CI runs the same Lisp as the dev machine. `apt-get install sbcl'
      # gave Debian's package -- 2.2.9, from 2022 -- which silently drifted four
      # years behind local (2.6.6) and failed where local passed. Bump this one
      # line to move both together.
      SBCL_VERSION: '2.6.6'
    steps:
      - name: Checkout parsifal (this repo, pushed ref)
        uses: actions/checkout@v4
        with:
          path: parsifal

      - name: Checkout cgraph
        uses: actions/checkout@v4
        with:
          repository: bruceseely/cgraph
          path: cgraph
          token: ${{ secrets.STACK_CHECKOUT_TOKEN }}

      - name: Checkout cg-from-parse
        uses: actions/checkout@v4
        with:
          repository: bruceseely/cg-from-parse
          path: cg-from-parse
          token: ${{ secrets.STACK_CHECKOUT_TOKEN }}

      - name: Install SBCL ${{ env.SBCL_VERSION }}
        run: |
          set -eu
          url="https://downloads.sourceforge.net/project/sbcl/sbcl/${SBCL_VERSION}/sbcl-${SBCL_VERSION}-x86-64-linux-binary.tar.bz2"
          curl -sSL --fail -o /tmp/sbcl.tar.bz2 "$url"
          tar -xjf /tmp/sbcl.tar.bz2 -C /tmp
          (cd "/tmp/sbcl-${SBCL_VERSION}-x86-64-linux" && sudo sh install.sh)
          sbcl --version

      - name: Cache Quicklisp
        uses: actions/cache@v4
        with:
          path: ~/quicklisp
          key: quicklisp-${{ runner.os }}-sbcl-${{ env.SBCL_VERSION }}

      - name: Bootstrap Quicklisp + SBCL init
        run: |
          if [ ! -f ~/quicklisp/setup.lisp ]; then
            curl -sSL -o /tmp/quicklisp.lisp https://beta.quicklisp.org/quicklisp.lisp
            sbcl --non-interactive --load /tmp/quicklisp.lisp \
                 --eval '(quicklisp-quickstart:install)'
          fi
          # Always (re)write ~/.sbclrc so it works on a Quicklisp cache hit too:
          # load Quicklisp, and unlock the CL package -- parsifal and cgraph
          # declare CL-package symbols (e.g. NTH as special, TYPE as a reader)
          # that are package-locked by default. Mirrors the dev machine's ~/.sbclrc.
          printf '%s\n' \
            '(load (merge-pathnames "quicklisp/setup.lisp" (user-homedir-pathname)))' \
            '(sb-ext:unlock-package :common-lisp)' > ~/.sbclrc

      - name: Run whole-stack gate
        run: bash cg-from-parse/test/run-all-suites.sh
